(* wrapper for emacs client that starts an emacs daemon if one isn't
running 
build with: ocamlbuild -p
 *)
open Core
module PI = Unix.Process_info
	     
(*let check_for_running_emacs () : bool =
  
       
let main () : unit =

;;
let _ = main ()*)
